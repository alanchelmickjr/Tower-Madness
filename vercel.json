{
  "buildCommand": "pip3 install pygbag && python3 -m pygbag --build main_web.py",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Using Python build'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
